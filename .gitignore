# 원본 온톨로지, 용량 큼 → README에 다운로드 링크만 남김
data_prep/mondo.json          
*.pt
*.npy
# BioLORD 임베딩 캐시(추후 생성될 파일), 팀원마다 로컬 재생성 
embeddings/                
# Langfuse Key and URL
.env
# uv-managed virtual environment
.venv/
# Sensitive patient data — kept locally only, never tracked in git
pending_diag/
# Python bytecode cache
__pycache__/
# Module 2 run outputs — derived from real patient data, kept locally only
modules/module2_deterministic/output/
